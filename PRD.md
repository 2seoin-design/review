# Matchip PRD

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | Matchip (Match + Trip) |
| 한 줄 설명 | 시간 맞추기 어려운 친구들이, 각자 편한 시간에 원하는 국내 여행지를 선택해두고, 하나라도 겹치면 자동으로 여행지가 확정되는 서비스 |
| 유형 | 개인 프로젝트 / 포트폴리오용 |
| 난이도 | 회원가입 + DB 연동이 있는 풀스택 |
| 개발 기간 | 2주 |

---

## 2. 사용자 시나리오

- 친구들끼리 여행을 가고 싶지만 각자 시간이 맞지 않아, 실시간으로 모여 목적지를 정하기 어려운 상황
- 한 명이 방을 만들고, 카톡 등으로 방 코드를 공유
- 친구들은 각자 편한 시간에 코드로 입장해, 그 자리에서 닉네임을 설정 (방장/참여자 구분 없이 동일한 입장 플로우)
- 입장한 순서대로 1, 2, 3... 번호가 부여됨
- 회원가입 없이 운영되므로, 재접속 시에는 코드 + 닉네임으로 본인 확인
- 방 생성 시 참여 인원 수를 설정 (이후 수정 불가)
- 설정 인원 전원이 "닉네임 등록 + 여행지 선택"까지 마쳐야 참여 완료로 카운트됨
- 방장은 언제든 강제 마감 가능
- 여행지 확정 후 5일 뒤 방 자동 삭제

---

## 3. 핵심 기능 목록

### 필수 기능 (오늘/이번 개발 사이클 범위)
- 방 생성 (방 이름, 인원 수 설정 → 영문+숫자 6자리 코드 발급)
- 코드로 방 입장 + 닉네임 등록 (방장/참여자 구분 없음)
- 국내 여행지 목록에서 복수 선택 (광역시/도 단위, 10~15곳)
- 카카오맵 기반 지역 마커 → 클릭 시 레이어 팝업으로 추천 루트 표시 → 팝업 내 "이 지역 선택하기" 버튼
- 겹침 감지 → 전원 참여 완료 시 자동 확정
- 확정 시 접속 중인 참여자에게 토스트 알림
- 확정 결과 화면 ("OO 여행지로 확정!" + 해당 지역 추천 루트 표시)
- 겹치는 곳이 없을 경우 재선택 기회 제공 (최대 3회)
- 3회 소진 후에도 미확정 시, 방장이 수동으로 삭제/재생성 결정
- 방장 강제 마감 기능
- 확정 후 5일 뒤 방 자동 삭제

### 선택 기능 (로드맵)
- 숙소 추천
- 참여자별 선택 수정/재투표 (3회 제한 외 추가 기능)
- 지역별 상세 추천 코스 페이지 고도화
- 브라우저 종료 후에도 수신 가능한 Web Push 알림
- 관광공사 Open API 연동 실패 시 수동 데이터 폴백 체계 고도화

---

## 4. 기술 스택

| 영역 | 스택 | 비고 |
|---|---|---|
| 백엔드 | FastAPI (Python) | Vercel Serverless Functions로 배포 |
| 프론트엔드 | 순수 HTML/CSS/JS | 모바일 반응형 필수 |
| DB | Supabase (PostgreSQL, 무료 티어) | 서버리스 환경과 호환, 데이터 영속성 확보 |
| 지도 API | 카카오맵 API | 국내 지역 특화, 무료 사용량 넉넉 |
| 외부 데이터 | 한국관광공사 Open API (TourAPI) | 지역별 추천 루트 자동 생성 |
| 배포 | Vercel | 프론트+백엔드 함께 배포, 무료 티어 |
| 운영 비용 | 완전 무료 운영 (모든 스택 무료 티어 내 운용) | |

---

## 5. 화면 구성

1. **방 생성 화면**
   - 방 이름 입력
   - 인원 수 입력
   - 방 생성 버튼

2. **코드 입장 화면**
   - 방 코드 입력
   - 닉네임 입력
   - 입장 버튼

3. **방 메인 화면**
   - 방 이름, 참여 인원 현황 (예: 3/5명 참여)
   - 현재 참여자 닉네임 목록
   - 여행지 선택하러 가는 버튼/링크
   - 방장만 보이는 강제 마감 버튼

4. **여행지 선택 화면**
   - 카카오맵 위에 여행지 마커 표시
   - 마커 클릭 → 레이어 팝업(추천 루트 미리보기) → "이 지역 선택하기" 버튼
   - 복수 선택 가능

5. **확정 결과 화면**
   - "OO 여행지로 확정!" 멘트
   - 선택 시 보았던 해당 지역 추천 루트 표시

---

## 6. 상세 기능 명세

### 겹침 판정 로직
- 설정된 인원 전원이 "닉네임 등록 + 여행지 선택 완료" 상태가 되어야 겹침 판정 시작
- 판정 결과 겹치는 지역이 있으면 즉시 확정
- 겹치는 지역이 없으면 재선택 기회 제공 (최대 3회)
- 3회까지 겹치는 곳이 없으면, 방장이 수동으로 방 삭제 또는 재생성 여부 결정

### 방 코드
- 형식: 영문+숫자 6자리 (예: A3F9K2)

### 여행지 데이터
- 범위: 광역시/도 단위, 10~15곳
- 추천 루트: 한국관광공사 Open API(TourAPI)로 자동 생성

### 알림
- 확정 시, 현재 접속 중인 참여자에게 토스트 알림 (브라우저 종료 시 수신 불가 — Web Push는 로드맵)

### 방 생명주기
- 확정 후 5일 뒤 자동 삭제
- 인원 수는 방 생성 후 수정 불가

---

## 7. 디자인 가이드

- **컬러**: 전체 톤은 청량한 푸른 계열, 포인트 컬러로 쨍한 주황
- **폰트**: 깔끔하고 모던한 느낌 (Pretendard 또는 SUIT 계열, self-host)
- **레이아웃**: 모바일 반응형 필수 (카톡 공유 특성상 모바일 접속 비중 높음 — PC/모바일 모두 대응)

---

## 8. 제약사항

- 개발 기간: 2주
- 모바일 반응형 필수 구현
- 완전 무료 운영 (Vercel, Supabase, 카카오맵 API, 관광공사 Open API 모두 무료 티어 내에서 운용)
- 회원가입 없이 코드 기반으로만 운영 (개인정보 최소 수집)
